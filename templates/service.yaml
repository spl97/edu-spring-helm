apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-sample.fullname" . }}
  labels:
    {{- include "spring-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-sample.selectorLabels" . | nindent 4 }}
